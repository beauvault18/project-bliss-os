node_modules/
dist/
dist-electron/
*.log
.DS_Store
# macOS AppleDouble sidecar files (created on non-HFS volumes like the T7 Shield)
._*

# Secrets / local env (defensive — AI key is stored via Electron safeStorage, not here)
.env
.env.*
*.local
